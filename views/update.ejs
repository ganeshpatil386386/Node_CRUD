<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/app.css">
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>

    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/read">Read users</a>

        </div>
        <h1 class="text-3xl tracking-tighter mb-3"> Update User</h1>
        <form action="/edit/<%= userUp._id %>" method="post">
            <input
            class="px-5 py-2 border-2 border-zinc-800 
            outline-none bg-transparent rounded-lg"
            placeholder="username"
            value="<%= userUp.name %>"
            name="name"
            type="text">

            <input
            class="px-5 py-2 border-2 border-zinc-800 
            outline-none bg-transparent rounded-lg"
            placeholder="email"
            name="email"
            value="<%= userUp.email %>"
            type="email">

            <input
            class="px-5 py-2 border-2 border-zinc-800 
            outline-none bg-transparent rounded-lg"
            placeholder="image url"
            name="imageurl"
            value="<%= userUp.imageurl %>"
            type="text">

            <input 
             class="px-5 py-2 bg-yellow-500 rounded-lg ml-3"
             type="submit" value="Update user">
        </form>
    </div>
</body>
</html>